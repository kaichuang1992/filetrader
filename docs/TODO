Things still to do:

== 1.0 ==
- make that is there now actually work

== 2.0 ==
- make it possible to see whether or not the invited user actually downloaded the file (email share)
- make it possible to expire tokens (email share)
- implement automatic class loader
  - add library.config.php file or something to point to (external) PHP/JS
    libraries
- fix external OAuth access to resources
- custom CSS/JS for Plupload instead of default
- don't assume we run on Unix-like systems
- implement 3-legged OAuth

